<header>
	<nav class="navbar navbar-expand-lg bg-body-tertiary">
		<div class="container">
			<a class="navbar-brand text-dark fw-bold" href="{{ path('app_home') }}">
				<i class="fa-solid fa-guitar"></i>
				<span class="text-warning">[</span>{{ settings.siteName }}<span class="text-warning">]</span>
			</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<a class="nav-link active" aria-current="page" href="{{ path('app_home') }}"><i class="fa-solid fa-house"></i> Accueil</a>
					</li>
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							<i class="fa-solid fa-music"></i> Tonalités
						</a>
						<ul class="dropdown-menu">
							{% for ton in tonalites %}
								<li>
									<a class="dropdown-item" href="{{ path('app_tonalite_show', {'slug': ton.slug}) }}">{{ ton.title }}</a>
								</li>
							{% endfor %}
						</ul>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_article_index') }}"><i class="fas fa-newspaper"></i> Articles</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_apropos') }}"><i class="fa-solid fa-circle-info"></i> A propos</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_contact') }}"><i class="fa-regular fa-envelope"></i> Contact</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_search') }}"><i class="fa-solid fa-magnifying-glass"></i> Recherche</a>
					</li>
				</ul>
				{% if app.user and is_granted('ROLE_ADMIN') %}
					<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
						<li class="nav-item">
							<a class="nav-link" href="{{ path('admin') }}"><i class="fa-solid fa-gear"></i> Admin</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_logout') }}"><i class="fa-solid fa-right-from-bracket"></i> Déconnexion</a>
						</li>
					</ul>
				{% endif %}
			</div>
		</div>
	</nav>
</header>
