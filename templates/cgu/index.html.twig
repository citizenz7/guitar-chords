{% extends 'base.html.twig' %}

{# {% block title %}{{ cgu.title }}{% endblock %} #}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col">
                <h1 class="text-center fw-bold border rounded bg-dark text-white p-3">{{ cgu.mainTitle }}</h1>
                <div class="row my-5">
                    <div class="col">
                        {{ cgu.content|replace({'<div>': '', '</div>': '',})|raw|nl2br }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
